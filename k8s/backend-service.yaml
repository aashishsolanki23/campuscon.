apiVersion: v1
kind: Service
metadata:
  name: campuscon-backend
  namespace: campuscon
  labels:
    app: campuscon-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: campuscon-backend
