# This file contains secret configuration values
# In production, these should be environment variables or external secrets

# Database credentials
db:
  username: campuscon
  password: JumpyFrogs&SingingBirds23

# JWT Configuration
jwt:
  secret: campuscon-super-secret-jwt-signing-key-2024-development-only
  expiration: 86400000  # 24 hours

# Mail Configuration
mail:
  username: your-email@gmail.com
  password: your-app-password

# AWS Configuration
aws:
  access-key: your-aws-access-key
  secret-key: your-aws-secret-key
  region: ap-south-1
  s3:
    bucket: campuscon-bucket

# Firebase Configuration
firebase:
  credentials-file: /path/to/firebase-credentials.json

# OAuth2 Secrets (for production)
oauth2:
  google:
    client-id: your-google-client-id
    client-secret: your-google-client-secret
  facebook:
    client-id: your-facebook-client-id
    client-secret: your-facebook-client-secret
